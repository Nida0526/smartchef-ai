{
  "buildCommand": "npm run build --prefix frontend && cp -r frontend/dist dist",
  "outputDirectory": "dist",
  "installCommand": "npm install --prefix frontend",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
