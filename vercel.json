{
  "buildCommand": "npm run build --prefix frontend && rm -rf public && cp -r frontend/dist public",
  "outputDirectory": "public",
  "installCommand": "npm install --prefix frontend",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
